name: called	

on:
  workflow_call:
    inputs:
      msg:
        required: false
        type: string
      tag: 
        type: string
    secrets:
      registry_username:
        required: false
      registry_password:
        required: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:      
      - uses: actions/checkout@v2
      - name: Print 
        run: echo "${{inputs.msg}}"

  do-something-else:
    runs-on: ubuntu-latest

    steps:
    - run: echo "${{inputs.msg}}"

# This is a basic workflow that is manually triggered

name: clean

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  
  push: 
    branches: [main]

  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      name:
        # Friendly description to be shown in the UI instead of 'name'
        description: 'Person to greet'
        # Default value if no value is explicitly provided
        default: 'World'
        # Input has to be provided for the workflow to run
        required: true

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "greet"
  clean:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    env:
      CORE_ENVIRONMENT: ${secrets.GH_TOKEN}
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    - uses: actions/checkout@v2
    # Runs a single command using the runners shell
    - name: try delete
      id: runID
      run: |
        gh api -X GET /repos/${{ github.repository }}/actions/runs | jq -r '.workflow_runs[] | select(.name="pages build and deployment",.conclusion!="failure")| .id' | xargs -n1 -I % gh api -X DELETE /repos/${{ github.repository }}/actions/runs/%
      env:
        GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
